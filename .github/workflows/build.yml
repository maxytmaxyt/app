name: Build Android APK

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # --------------------
      # 🔄 Checkout Repository
      # --------------------
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # --------------------
      # ☕ Setup JDK
      # --------------------
      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # --------------------
      # 📦 Gradle Cache
      # --------------------
      - name: ⚡ Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # --------------------
      # 🏷 Bump Version & Update Files
      # --------------------
      - name: 🔢 Bump Version
        id: version
        run: |
          git fetch --tags
          LAST_TAG=$(git tag --list "v*" --sort=-v:refname | head -n 1)

          if [ -z "$LAST_TAG" ]; then
            MAJOR=0
            MINOR=1
            PATCH=0
          else
            IFS='.' read -r MAJOR MINOR PATCH <<< "${LAST_TAG#v}"
            PATCH=$((PATCH + 1))
          fi

          VERSION="v$MAJOR.$MINOR.$PATCH"
          echo "✅ Neue Version: $VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=${VERSION#v}" >> $GITHUB_ENV

          # --------------------
          # ✍️ Gradle.properties aktualisieren (ohne andere Zeilen zu löschen)
          # --------------------
          # AndroidX Flags sicherstellen
          grep -qxF "android.useAndroidX=true" gradle.properties || echo "android.useAndroidX=true" >> gradle.properties
          grep -qxF "android.enableJetifier=true" gradle.properties || echo "android.enableJetifier=true" >> gradle.properties

          # Version setzen oder ersetzen
          if grep -q "^version=" gradle.properties; then
            sed -i "s/^version=.*/version=${VERSION#v}/" gradle.properties
          else
            echo "version=${VERSION#v}" >> gradle.properties
          fi

          # --------------------
          # ✍️ build.gradle VersionName & VersionCode setzen
          # --------------------
          sed -i "s/versionCode [0-9]\+/versionCode $PATCH/" app/build.gradle || echo "versionCode $PATCH" >> app/build.gradle
          sed -i "s/versionName \".*\"/versionName \"${VERSION#v}\"/" app/build.gradle || echo "versionName \"${VERSION#v}\"" >> app/build.gradle

          # --------------------
          # 💾 Commit + Tag
          # --------------------
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add gradle.properties app/build.gradle
          git commit -m "chore: bump version to ${VERSION} [skip ci]" || echo "Nothing to commit"
          git tag ${VERSION}
          git push origin main --tags

      # --------------------
      # 🔧 Check Gradle Wrapper
      # --------------------
      - name: 🏗️ Ensure Gradle Wrapper
        run: |
          if [ ! -f "./gradlew" ]; then
            echo "🚀 Wrapper fehlt – erstelle gradlew..."
            gradle wrapper --gradle-version 8.4
            chmod +x gradlew

            echo "💾 Commit Wrapper ins Repo..."
            git config --global user.name "github-actions[bot]"
            git config --global user.email "github-actions[bot]@users.noreply.github.com"
            git add gradlew gradlew.bat gradle/wrapper/*
            git commit -m "chore: add Gradle wrapper [skip ci]" || echo "Nothing to commit"
            git push
          else
            echo "✅ Wrapper vorhanden"
          fi

      # --------------------
      # 🏗️ Build APK
      # --------------------
      - name: 📦 Build Debug APK
        run: ./gradlew assembleDebug

      # --------------------
      # 📤 Upload APK
      # --------------------
      - name: 🚀 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DeviceInfoApp-${{ env.VERSION }}
          path: app/build/outputs/apk/debug/app-debug.apk
